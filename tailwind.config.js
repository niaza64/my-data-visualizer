/** @type {import('tailwindcss').Config} */
module.exports = {
  // Update the `content` array to include the paths to all of your template files
  content: [
    "./src/**/*.{html,js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {
      // Here you can extend the default Tailwind styles,
      // or add new ones
    },
  },
  plugins: [
    // Here you can add Tailwind CSS plugins
  ],
}
